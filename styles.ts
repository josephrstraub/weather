import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  cityName: {
    fontSize: 30,
    fontWeight: 'bold',
    marginTop: 100,
    textAlign: 'center',
  },
  container: {
    flex: 1,
  },
  textInput: {
    backgroundColor: '#FFFFFF',
    height: 50,
    marginHorizontal: 15,
  },
});
